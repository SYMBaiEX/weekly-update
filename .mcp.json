{
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_USER_TOKEN": "${WEEKLY_UPDATE_SLACK_TOKEN}",
        "SLACK_TEAM_ID": "${WEEKLY_UPDATE_SLACK_TEAM_ID}"
      }
    }
  }
}
